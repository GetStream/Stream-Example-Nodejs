<div id="wrapper">
    <div class="container-pins">
        <% activities.forEach(function(activity){  %>
            <%if (activity.pin) {%>
                <%- include pin.ejs %>
            <% } else{ %>
            	<%- include follow.ejs %>
            <% } %>
        <% }) %>
    </div>
</div>